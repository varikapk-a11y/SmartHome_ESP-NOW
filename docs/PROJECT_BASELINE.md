# ПАСПОРТ БАЗОВОЙ ВЕРСИИ ПРОЕКТА

## Основные сведения
*   **Дата создания:** 4 февраля 2026 года
*   **Статус:** Рабочий прототип. Сборка успешна для обеих плат.
*   **Описание:** Система умного дома на ESP-NOW. Центральный хаб (ESP32) с веб-интерфейсом управляет удалённым узлом (ESP32-C3), включая/выключая его светодиод.

## Среда разработки и цели сборки
*   **PlatformIO Core:** `espressif32`
*   **Framework:** `arduino`
*   **Хаб (ESP32):** Плата `esp32dev`, окружение `esp32-wroom-32u`
*   **Узел (ESP32-C3):** Плата `esp32-c3-devkitm-1`, окружение `esp32c3`

## Версии библиотек (ВАЖНО! ЗАФИКСИРОВАТЬ)
Эти версии гарантируют стабильную сборку проекта. При обновлении библиотек проверяйте совместимость.

### Проект "Хаб" (`firmware/hub_esp32`)
*   `bblanchon/ArduinoJson @ 6.21.3`
*   `me-no-dev/ESPAsyncWebServer @ 3.6.0`
*   **Примечание:** Библиотека `AsyncTCP` не указана явно, но является обязательной зависимостью для `ESPAsyncWebServer`. PlatformIO установит её автоматически совместимую версию.

### Проект "Узел" (`firmware/node_esp32c3`)
*   Использует ядро ESP32: `https://github.com/espressif/arduino-esp32.git`
*   Игнорируется библиотека: `ArduinoOTA`

## Конфигурация устройств
*   **SSID точки доступа хаба:** `ESP32-Hub-Test`
*   **Пароль:** `test12345678`
*   **MAC-адрес узла (зашит в коде хаба):** `AC:EB:E6:49:10:28`
*   **MAC-адрес хаба (для замены в коде узла):** Узнать из Serial Monitor после прошивки хаба.
*   **Пин светодиода на узле:** `GPIO8` (активный LOW на ESP32-C3 DevKit)
*   **Скорость монитора узла:** `115200` бод

## Инструкция первого запуска
1.  **Собрать и прошить хаб:** Откройте проект `firmware/hub_esp32` в PlatformIO -> Build -> Upload.
2.  **Узнать MAC хаба:** Откройте Serial Monitor (115200). Где-то в логе (или добавьте `Serial.println(WiFi.softAPmacAddress());` в `setup()`) будет выведен реальный MAC-адрес в формате `XX:XX:XX:XX:XX:XX`.
3.  **Исправить адрес в коде узла:** В файле `firmware/node_esp32c3/src/main.cpp` найдите строку с `hubMacAddress` и замените массив на реальный MAC хаба. Например, `{0x9C, 0x9C, 0x1F, 0xC7, 0x2D, 0x94}`.
4.  **Собрать и прошить узел:** Откройте проект `firmware/node_esp32c3` -> Build -> Upload.
5.  **Подключиться к системе:** На клиенте (телефон/ПК) подключитесь к Wi-Fi сети `ESP32-Hub-Test`. В браузере откройте IP-адрес, который вывел хаб в Serial Monitor (обычно `192.168.4.1`).
6.  **Проверить работу:** Используйте кнопки на веб-странице для управления светодиодом на узле.

## Известные особенности и возможные проблемы
1.  Хаб для работы ESP-NOW использует режим `WiFi.mode(WIFI_AP_STA)` (точка доступа + клиент).
2.  Структура данных `esp_now_message` должна быть в точности одинаковой в коде хаба и узла.
3.  В коде используется актуальный API ESP-NOW для Arduino Core версии 3.4.0+.
4.  **Важно!** В конфигурации хаба отсутствует библиотека `AsyncTCP` в `lib_deps`. PlatformIO установит её сам, но для полной воспроизводимости рекомендуется добавить строку `ESP32 Async TCP @ 1.2.2` в раздел `lib_deps` файла `hub_platformio.ini`.
5.  Библиотека `ArduinoOTA` на узле отключена через `lib_ignore`, что может быть важно для экономии памяти.
6.  ------------------------------------------------

7.  ПАСПОРТ ПРОЕКТА: УМНЫЙ ДОМ НА ESP-NOW (Версия 2.0)
Дата обновления: 5 февраля 2026 года
Статус: Рабочий прототип с поддержкой двух датчиков. Ведутся работы по обновлению веб-интерфейса хаба.

🎯 Текущая цель
Создать масштабируемую систему на ESP-NOW, где хаб (ESP32) через веб-интерфейс принимает данные с нескольких узлов, оснащённых датчиками, и управляет ими.

✅ Достигнутые результаты (Версия 2.0)
Узел (ESP32-C3) полностью функционирует:

Решена проблема с Serial Monitor через USB-CDC (флаги ARDUINO_USB_CDC_ON_BOOT=1 в platformio.ini).

Подключены и работают датчики AHT20 (температура, влажность, адрес 0x38) и BMP280 (температура, давление, адрес 0x76).

Данные отправляются в новом, вложенном формате JSON (см. пример ниже).

Сохранена обратная совместимость для команд управления (LED_ON, LED_OFF, GET_STATUS).

Пример отправляемых данных (новый формат):

json
{
  "node": 101,
  "sensor": "AHT20+BMP280",
  "AHT20": {"temp": 27.0, "hum": 24.6},
  "BMP280": {"temp": 27.3, "press_mmHg": 759.7}
}
Хаб (ESP32) готов к обновлению:

Требуется доработка кода для парсинга нового JSON и отображения данных с двух датчиков в веб-интерфейсе.

Готовый код для хаба будет предоставлен после успешного теста нового формата от узла.

🔧 Актуальная конфигурация устройств
MAC-адрес хаба (из чата): 9C:9C:1F:C7:2D:94

MAC-адрес узла (из чата): AC:EB:E6:49:10:28

Пины I2C для ESP32-C3: SDA=GPIO1, SCL=GPIO0

Скорость монитора порта: 115200 бод (обязательно для USB-CDC на ESP32-C3).

📅 План развития (ближайшие шаги)
Закрепить успех: Убедиться, что узел стабильно отправляет данные в новом формате.

Обновить хаб: Прошить на ESP32-хаб новую версию кода с парсером вложенного JSON и обновлённым веб-интерфейсом.

Интеграционное тестирование: Проверить полный цикл: данные с узла → хаб → веб-страница.

Масштабирование: На основе новой структуры JSON добавить на веб-страницу хаба динамическую таблицу для отображения данных с 5+ устройств (следующий этап).
------------------------------



## 📍 ТЕКУЩЕЕ СОСТОЯНИЕ (АКТУАЛЬНО НА 06.02.2026):

**СИСТЕМА В РАБОЧЕМ СОСТОЯНИИ**

🟢 **СТАТУС:** БАЗОВАЯ СВЯЗЬ ВОССТАНОВЛЕНА, ПРОТОТИП ФУНКЦИОНИРУЕТ

### ✅ ЧТО РАБОТАЕТ:
- **Стабильная двусторонняя связь ESP-NOW** между хабом (ESP32) и узлом (ESP32-C3) на основе универсального JSON-протокола.
- **Веб-интерфейс управления:** Хаб создаёт точку доступа `SmartHome-Hub`. Через страницу `192.168.4.1` можно:
    - Управлять светодиодом на узле (кнопки ВКЛ/ВЫКЛ).
    - Запрашивать актуальные данные датчиков.
    - Видеть обновляемые в реальном времени показания AHT20 (температура, влажность) и BMP280 (температура, давление).
- **Автономная работа узла:** Узел каждые 30 секунд отправляет данные датчиков, обрабатывает входящие команды и отправляет подтверждения.

### 🏗️ АРХИТЕКТУРА И ПРОТОКОЛ:
- **Режимы Wi-Fi:**
    - **Хаб:** `WiFi.mode(WIFI_AP_STA)` (сначала `WIFI_AP` для веба, затем `WIFI_AP_STA` для ESP-NOW).
    - **Узел:** `WiFi.mode(WIFI_STA)`.
- **Универсальная структура сообщений:**
    ```cpp
    typedef struct esp_now_message {
        char json[192];    // Универсальное поле для JSON любой команды или данных
        uint8_t sender_id; // ID отправителя
    } esp_now_message;
    ```
- **Ключевые MAC-адреса:**
    - MAC Хаба (STA, для ESP-NOW): `9C:9C:1F:C7:2D:94`
    - MAC Узла: `AC:EB:E6:49:10:28`

### 📈 СЛЕДУЮЩИЕ ШАГИ (В РАЗРАБОТКЕ):
Развитие проекта ведётся согласно плану в `ROADMAP.md` (Версия 0.2 и далее). Текущий фокус — на улучшение веб-интерфейса, добавление поддержки нескольких узлов и повышение надёжности связи.