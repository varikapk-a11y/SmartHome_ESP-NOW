markdown
# Настройка среды разработки для проекта "Умный дом на ESP-NOW"

## 🚀 Быстрый старт для разработки

Этот проект состоит из **двух независимых прошивок (firmware)**, которые необходимо собирать и прошивать отдельно. Для работы с ними требуется среда разработки PlatformIO.

### 📁 Структура проекта
SmartHome_ESP-NOW/
├── firmware/
│ ├── Hub/ # Прошивка для центрального хаба (ESP32)
│ └── Node/ # Прошивка для узла "Мастерская" (ESP32-C3)
└── docs/ # Документация проекта

text

## 🔧 Правильное открытие проектов в VSCode

**Платформа PlatformIO корректно работает только тогда, когда в VSCode открыта папка отдельного проекта (например, `firmware/Hub`).**

### ✅ Рекомендуемый способ: Открытие проектов по отдельности
1.  **Закройте** текущее окно VSCode (если открыта корневая папка репозитория).
2.  Запустите VSCode.
3.  В меню выберите: **File → Open Folder...**
4.  Найдите и выберите папку `firmware/Hub`.
5.  Теперь внизу окна и на боковой панели появятся кнопки PlatformIO для работы с хабом.
6.  **Повторите** шаги 1-5 для проекта узла, выбрав папку `firmware/Node`.

### 🔄 Альтернативный способ: Рабочая область (Workspace)
Можно добавить обе папки в одну рабочую область VSCode:
1.  Откройте палитру команд (**Ctrl+Shift+P**).
2.  Введите `Add Folder to Workspace` и выполните команду.
3.  Добавьте папку `firmware/Hub`, затем `firmware/Node`.
4.  Сохраните рабочую область через **File → Save Workspace As...**.

## 📦 Зависимости проектов (актуальные)

### Проект "Хаб" (`firmware/Hub/`)
Основные библиотеки для веб-интерфейса и связи:
```ini
lib_deps =
    bblanchon/ArduinoJson @ 6.21.3
    me-no-dev/ESPAsyncWebServer @ 3.6.0
    me-no-dev/AsyncTCP @ ^1.1.1
Проект "Узел" (firmware/Node/)
Библиотеки для датчиков, охраны и связи:

ini
lib_deps =
    adafruit/Adafruit Unified Sensor @ ^1.1.14
    adafruit/Adafruit AHTX0 @ ^2.0.5
    adafruit/Adafruit BMP280 Library @ ^3.0.0
    bblanchon/ArduinoJson @ 6.21.3
lib_ignore = ArduinoOTA
Важное примечание: В проекте узла теперь также используется ArduinoJson для парсинга команд от хаба и формирования пакетов данных системы охраны.

🛠️ Критические настройки для ESP32-C3 (Узел)
При работе с платой ESP32-C3 необходимо явно включить поддержку USB-CDC для корректной работы Serial Monitor (вывода отладочных сообщений в консоль).

Обязательно проверьте, что в файле firmware/Node/platformio.ini присутствует следующая строка:

ini
build_flags = -DARDUINO_USB_CDC_ON_BOOT=1
Без этого флага монитор последовательного порта будет пустым.

🔍 Проверка установки PlatformIO
Убедитесь, что расширение PlatformIO IDE установлено и активно:

Откройте панель расширений (Ctrl+Shift+X).

Найдите "PlatformIO IDE".

Убедитесь, что расширение установлено (не появится кнопка "Install").

⚙️ Сборка, прошивка и отладка
После правильного открытия проекта в VSCode станут доступны инструменты PlatformIO.

Через панель статуса (внизу окна):
✓ (Build) — проверить и собрать проект.

→ (Upload) — собрать и прошить на подключенную плату.

↔ (Serial Monitor) — открыть монитор порта для отладки (115200 бод).

Через боковую панель PlatformIO:
Нажмите на значок PlatformIO (жук) в левой панели. В разделе "PROJECT TASKS" вашего проекта (например, esp32-c3-devkitm-1) будут доступны команды: Build, Upload, Clean, Monitor.

🐛 Решение частых проблем
Проблема	Вероятная причина и решение
Кнопки PlatformIO не отображаются	Открыта корневая папка репозитория. Закройте VSCode и откройте папку firmware/Hub или firmware/Node отдельно.
Ошибки при сборке (missing dependencies)	Проверьте подключение к интернету. PlatformIO автоматически загрузит библиотеки, указанные в lib_deps.
Пустой Serial Monitor для ESP32-C3	Не установлен флаг -DARDUINO_USB_CDC_ON_BOOT=1 в platformio.ini узла. Добавьте его.
Project tasks не загружаются	Убедитесь, что в корне открытой папки есть файл platformio.ini.
📚 Рекомендации по разработке
Работайте с проектами раздельно. Всегда открывайте Hub или Node как отдельный проект в VSCode.

Сверяйтесь с базовой версией. Перед обновлением версий библиотек или внесением архитектурных изменений проверяйте документ PROJECT_BASELINE.md.

Тестируйте на реальном железе. После изменений в коде, особенно связанных с охраной (GPIO3, GPIO4) или протоколом ESP-NOW, обязательно протестируйте работу на физических устройствах.

Обновляйте документацию. Если вы добавили новую библиотеку или изменили критическую настройку (например, пины), внесите соответствующие правки в PROJECT_BASELINE.md и этот файл.

text

### ✨ Основные улучшения в новой версии:

*   **Структура и ясность:** Добавлено оглавление и чёткое разделение на логические блоки.
*   **Актуальность:** Внесены библиотеки и конфигурации, соответствующие **текущей рабочей версии 2.5** проекта (с охраной).
*   **Ключевое предупреждение:** Явно выделен и подробно описан **критически важный флаг `DARDUINO_USB_CDC_ON_BOOT=1`** для ESP32-C3, который часто упускают.
*   **Устранение дублирования:** Убраны общие фразы, оставлено только практическое руководство.
*   **Удобство использования:** Частые проблемы сведены в наглядную таблицу для быстрого поиска решения.
